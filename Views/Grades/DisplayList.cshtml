@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model List<StudentPortal.Models.Entities.Grade>
<h1>All Student Marks</h1>
@if (TempData["AlertMessage"] != null)
{
    <div class="alert alert-success">
        <strong>Sucess!</strong> @TempData["AlertMessage"]
    </div>
}

<table class="table">
    <thead>
        <tr>
            <th>Grade ID</th>
            <th>Student ID</th>
            <th>Course ID</th>
            <th>Marks</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var grade in Model)
        {
            <tr>
                <td>@grade.GID</td>
                <td>@grade.SID</td>
                <td>@grade.CourseID</td>
                <td>@grade.Score</td>

                <td>
                    <a class="btn btn-primary" asp-controller="Grades" asp-action="EditList" asp-route-sid="@grade.GID">EDIT</a>
                </td>
            </tr>
        }
    </tbody>
</table>